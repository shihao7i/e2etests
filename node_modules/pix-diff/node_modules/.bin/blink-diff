#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../blink-diff/bin/blink-diff" "$@"
  ret=$?
else 
  node  "$basedir/../blink-diff/bin/blink-diff" "$@"
  ret=$?
fi
exit $ret
